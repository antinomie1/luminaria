# Luminaria

一个用于构建 Wayland 混成器的 C++ 库。抽象程度介于 wlroots 与"手写 X11 窗口管理器"
之间：库负责机制，混成器保留对每一帧的掌控权。

## Language

**混成器 (Compositor)**：
使用本库写出的那个程序。它拥有全局坐标系、焦点与每一帧的最终决定权。
_Avoid_: 窗口管理器、WM、合成器（"合成"专指像素混合这一步，见「合成」）

**核心层 (Core)**：
库中不含策略的那一半——协议对象、后端、渲染器。同样的输入永远给出同样的输出，
不替混成器做任何决定。
_Avoid_: 底层、backend（backend 是核心层里的一个子系统，不是核心层本身）

**外壳层 (Shell)**：
库中有主见的那一半——每个混成器都得写一遍、写错就是 bug 的那些公共账本（帧调度、
damage 记账、光标合成、焦点仲裁）。混成器可以整块不用它。
_Avoid_: 框架、运行时；也不要与 `xdg_shell` / `layer_shell` 协议里的 "shell" 混用

**摆放策略 (Placement policy)**：
窗口摆在哪、怎么平铺、工作区怎么切——**明确不属于本库**，每个混成器自己实现。
_Avoid_: 布局（"布局"在本库里专指 `OutputLayout`，即输出在全局坐标系里的位置）

**合成 (Compositing)**：
把多个表面的像素混合进一块渲染目标这一步，由渲染器完成。不是"混成器"这个程序的同义词。

**摆位 (Placement)**：
一次"把某个表面画在这里、这么大、带这个变换"的记录。混成器每帧现搭一串摆位交给外壳层。
_Avoid_: Layer、图层（`layer` 已经属于 `zwlr_layer_shell` 的四个层）；Node、节点（没有树）

**帧 (Frame)**：
外壳层为一次上屏所做的记账——摆位列表、repaint 区域、fence 编排。它活在输出上以复用内存，
但不跨帧保存任何窗口信息。
_Avoid_: 帧缓冲（那是 KMS 的 framebuffer）

**表面 (Surface)**：
客户端提交像素的对象，即 `wl_surface`。表面坐标不是 buffer 像素。
_Avoid_: 窗口（窗口是混成器的概念，一个窗口可以是一棵表面树）
