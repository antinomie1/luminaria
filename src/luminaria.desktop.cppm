// Protocols reserved for desktop-shell components. None is admitted to the
// core module because each enumerates or controls another client's objects.
export module luminaria.desktop;

export import luminaria;
export import :data_control;
export import :foreign_toplevel;
export import :input_method;
export import :session_lock;
export import :workspace;
